# 01 — Repository inventory

Audited commit: `b6fd8ea` (branch `main`, working tree clean). 439 tracked files (`git ls-files | wc -l`).
Every claim below is cited to the file that proves it. Docs were read for context only.

## 1. Tracked top-level areas

| Area | Files | Purpose | Evidence |
|---|---|---|---|
| `.claude-plugin/` | 2 | Plugin manifest (`plugin.json`) and self-hosted marketplace (`marketplace.json`, `source: "./"`) | `.claude-plugin/plugin.json:1-46`, `.claude-plugin/marketplace.json:5-10` |
| `.mcp.json` | 1 | Declares the single MCP server `engine` = `node ${CLAUDE_PLUGIN_ROOT}/dist/mcp.mjs` | `.mcp.json:3-12` |
| `dist/mcp.mjs` | 1 | Committed single-file ESM bundle of `packages/mcp` (8,382,958 bytes). This is what the plugin runs | `ls -la dist`, `packages/mcp/tsdown.config.ts` |
| `skills/` | 26 | 21 `SKILL.md` files plus 5 plan references | §4 |
| `agents/` | 2 | `creative-director`, `source-researcher` subagents | §5 |
| `packages/` | 228 | pnpm workspace: `schema`, `core`, `ingestion`, `media`, `renderer`, `platforms`, `voice`, `mcp` | §2 |
| `templates/` | 18 | 18 story templates, one `template.yaml` each (`ls templates`) | README says 18. `.claude/CLAUDE.md` says "13 templates", which is stale |
| `styles/` | 5 | 4 style packs (`minimal`, `editorial`, `technical`, `energetic`) + README | `ls styles` |
| `music/` | 6 | 4 bundled CC0 beds (`.m4a`), `catalog.json`, README; generated by `scripts/generate-music.mjs` | `scripts/generate-music.mjs:2-4` |
| `fonts/` | 19 | Inter, JetBrains Mono, Noto Sans (+ Arabic, Devanagari, JP) | `ls fonts` |
| `platform-specs/` | 6 | Versioned platform contracts: facebook-page-api, instagram, linkedin, tiktok, youtube-shorts | `ls platform-specs` |
| `schemas/` | 16 | JSON Schemas generated from zod (`pnpm schemas`). The largest is `video-spec.schema.json`, 27,475 B | `packages/mcp/src/paths.ts:15-32` |
| `examples/` | 24 | `readme-hero`, `readme-plan`, `reel-grammar`, `text-to-motion-graphic`, `demo-app` | `git ls-files examples` |
| `fixtures/`, `tests/` | 24 + 21 | Ingest golden snapshots and golden frames for the example | `git ls-files tests fixtures` |
| `scripts/` | 5 | Dev/CI CLI (not shipped as `bin/`) | §3 |
| `docs/` | 15 | HANDOFF, PLAN, PHASE4_EXIT, USER_CHECKLIST, contributing guides, media | — |
| `.githooks/pre-push` | 1 | Runs `node scripts/check.mjs --push`. There is no CI | `.githooks/pre-push:4` |
| `t.sh` | 1 | `cd ~/Desktop/plugin_knowledge_to_video && git pull --ff-only`: a personal helper committed to the repo | `t.sh:1-4` |
| Research | 7 | `deep-research-report*.md`, `reports/`, `research_notes/` | — |

**Absent:** no `commands/` directory and no `hooks/` directory (`git ls-files` has neither). Users reach every entry point as a skill (`/video-studio:<skill>`).

## 2. Packages

| Package | Purpose | Runtime deps (package.json) | Key entry |
|---|---|---|---|
| `@video-studio/schema` | zod v4 models (ContentIR, VideoSpec, RenderManifest, Brand, Policy, Template, PlatformContract, VideoLock, Style, Experiment, DemoScript, FormatGrammar, ShortCandidates, TranslationSheet), JSON Schema emit | `zod`, `yaml` (`packages/schema/package.json:9`) | `packages/schema/src/index.ts` |
| `@video-studio/core` | Project layout, `resolveInsideProject`, atomic fs, CAS, canonical JSON, data dir, SQLite ledger, generic `JobRunner` | `schema`, `p-queue` (`packages/core/package.json:9`) | `packages/core/src/project.ts:50,86,181`; `ledger.ts:206`; `jobs.ts:107` |
| `@video-studio/ingestion` | Extractors: text, markdown, PDF (`unpdf`), DOCX (`mammoth`), PPTX (`jszip`), URL/HTML (`defuddle`, Readability, `linkedom`), repo (`repomix` searchFiles + `@secretlint/core`), media | see `packages/ingestion/package.json:16-31` | `packages/ingestion/src/ingest.ts:223` |
| `@video-studio/media` | ffmpeg/ffprobe process layer, assembly, audio mix/loudnorm, captions (SRT/VTT/ASS), technical QA, whisper ASR, beat detection, letterbox, frames/SSIM | `schema`, `core` only; shells out to system binaries | `packages/media/src/ffmpeg.ts:69,185`; `compose.ts:274`; `qa.ts:127` |
| `@video-studio/renderer` | Scene renderers: ffmpeg drawtext renderer, footage renderer, HyperFrames renderer; renderer selection, scene cache | `@hyperframes/producer@0.8.78` (pinned, dev only; not bundled), `core`, `schema`, `media`, `platforms` (`packages/renderer/package.json:16-22`) | `packages/renderer/src/select.ts:42,155,224` |
| `@video-studio/platforms` | Loads `platform-specs/*.yaml`, geometry, layout zones | `schema` | `packages/platforms/src/registry.ts`, `zones.ts` |
| `@video-studio/voice` | TTS backends: `system` (macOS `say` / `espeak-ng`), `elevenlabs` (fetch), `silent`; backend selection, voice cache | `schema`, `core` | `packages/voice/src/synthesize.ts:52-75`; `system.ts:136-153`; `elevenlabs.ts:11` |
| `@video-studio/mcp` | MCP stdio server, render pipeline, jobs, every tool implementation | `@modelcontextprotocol/sdk`, `zod`, all workspace packages (`packages/mcp/package.json:11-21`) | `packages/mcp/src/main.ts:9-19`; `server.ts:101` |

## 3. Entry points

| Entry point | What it does | Evidence |
|---|---|---|
| `.claude-plugin/plugin.json` | `name: video-studio`, `version 0.1.0`, Apache-2.0, **5 `userConfig` secrets**: `runway_key`, `elevenlabs_key`, `heygen_key`, `fal_key`, `kling_key` (all `sensitive: true`, optional) | `.claude-plugin/plugin.json:9-45` |
| `.mcp.json` | Server `engine`: `node ${CLAUDE_PLUGIN_ROOT}/dist/mcp.mjs`. Env maps each userConfig to `RUNWAYML_API_SECRET`, `ELEVENLABS_API_KEY`, `HEYGEN_API_KEY`, `FAL_KEY`, `KLINGAI_API_KEY`. It does not pass `CLAUDE_PLUGIN_DATA` explicitly | `.mcp.json:3-12` |
| `dist/mcp.mjs` | Built from `packages/mcp/src/main.ts`. Redirects `console.log/info/debug` to stderr, then connects `StdioServerTransport` | `packages/mcp/src/main.ts:5-13` |
| `scripts/render-project.mjs` | Dev CLI: renders a project through the bundle (`--voice`, `--renderer`, `--quality`, `--no-burn-in`) | `scripts/render-project.mjs:13-26` |
| `scripts/smoke-mcp.mjs` | Spawns the bundle, checks the tool list, runs ingest and a tiny render | `scripts/smoke-mcp.mjs:1-8` |
| `scripts/check.mjs` | typecheck, tests, bundle freshness, smoke, plugin validate, golden frames (pre-push) | `scripts/check.mjs:2-6` |
| `scripts/diagnose-chrome.mjs` | Diagnoses headless Chrome launch for HyperFrames | `scripts/diagnose-chrome.mjs:2-4` |
| `scripts/generate-music.mjs` | Synthesises the CC0 music beds with ffmpeg `aevalsrc` | `scripts/generate-music.mjs:2-4` |

The repo root is also the plugin root, so `.mcp.json` doubles as a **project-scope** MCP config whenever Claude Code is opened in this repo. There, `${CLAUDE_PLUGIN_ROOT}` is not defined. This is the "engine CONNECTION_CLOSED duplicate" the user's memory notes (`.mcp.json:5`).

## 4. Skills (21)

All tool names use the prefix `mcp__plugin_video-studio_engine__`, abbreviated below as `…`. **No skill sets `disable-model-invocation`** (verified: `grep disable-model-invocation skills` finds nothing).

| Skill | Lines | Trigger (description) | allowed-tools | license / compat |
|---|---|---|---|---|
| adapt | 26 | `/video-studio:adapt`, "a 16:9 version", "a 15-second cut" | …adapt, …spec_validate, …storyboard_render, Read, Edit | yes / yes |
| analyze | 43 | reference reel to imitate, "how fast does it cut" | …analyze, Read | yes / yes |
| compare | 51 | two versions side by side | …compare, Read | yes / yes |
| create | 111 | "make a video", "turn this into a reel/short" | …ingest, …spec_validate, …render_submit, …job_status, …qa_run, …export, …review, Skill, Read, Write, Edit | yes / yes |
| demo | 32 | product demo of the user's running app | …demo, …schema_get, …doctor, Read, Write | yes / yes |
| diff | 42 | what changed between two renders | …diff, Read | yes / yes |
| doctor | 34 | "does my setup work" | …doctor | yes / yes |
| export | 46 | rebuild dist/, post copy, C2PA | …export, Read, Edit | yes / yes |
| ingest | 88 | make a video "from" a document/URL/repo/recording | …ingest, …transcribe, Read | yes / yes |
| lint | 91 | "is it ready for a platform" | …lint, …spec_validate, …render_submit, …job_status, Read, Edit | **missing both** (`skills/lint/SKILL.md:1-5`) |
| localize | 61 | "in Japanese", "a Hindi version" | …localize, …spec_validate, …storyboard_render, Read, Edit | yes / yes |
| plan | 240 (+630 in references) | script, storyboard, hook ideas | …ingest, …template_list, …template_get, …spec_scaffold, …brief_validate, …spec_validate, …storyboard_render, …schema_get, Read, Write, Edit, Agent | yes / yes |
| qa | 24 | "is the reel ready to post" | …qa_run, …review, Read | yes / yes |
| render | 111 | render, preview or export; after approval | …spec_validate, …render_submit, …job_status, …qa_run, …export, …doctor, …review, Read, Write, Edit | yes / yes |
| review | 56 | "how does the render look" | …review, Read | yes / yes |
| shorts | 87 | cut a long video into shorts | …ingest, …transcribe, …shorts, …schema_get, …spec_validate, Read, Write | yes / yes |
| test | 48 | golden-frame regression | …test, Read | yes / yes |
| tighten | 30 | clean up a talk recording | …tighten, …transcribe, …ingest, Read | yes / yes |
| validate | 34 | validate a spec | …spec_validate, …schema_get, Read | yes / yes |
| variants | 41 | A/B hooks and covers | …variants, …job_status, …schema_get, …lint, Read, Write | yes / yes |
| verify | 51 | are the claims backed | …verify, Read, Edit | yes / yes |

The tool `project_init` is not in any skill's `allowed-tools`: `ingest` creates projects implicitly (`packages/mcp/src/server.ts:164-167`).

## 5. Agents (2)

| Agent | Tools | Role | Evidence |
|---|---|---|---|
| `source-researcher` | Read, Grep, Glob | Writes a research brief of 400 words or fewer from `source/content-ir.json`, citing only refs from the IR | `agents/source-researcher.md:1-37` |
| `creative-director` | Read, Grep, Glob | Read-only critique of brief/spec/storyboard. Returns P1–P3 suggestions (8 or fewer) | `agents/creative-director.md:1-78` |

## 6. MCP tools (28, all in `packages/mcp/src/server.ts`)

`doctor`:109, `project_init`:124, `ingest`:146, `spec_validate`:179, `schema_get`:214, `template_list`:230, `template_get`:246, `brief_validate`:261, `spec_scaffold`:278, `storyboard_render`:329, `render_submit`:352, `job_status`:410, `qa_run`:427, `lint`:449, `export`:467, `verify`:486, `test`:503, `review`:522, `compare`:558, `diff`:580, `variants`:605, `adapt`:648, `transcribe`:671, `shorts`:695, `analyze`:727, `demo`:745, `localize`:764, `tighten`:787.
The committed bundle registers the same 28 names (grep of `dist/mcp.mjs`). There are no MCP resources or prompts, only tools. See `07-mcp-tool-audit.md`.

## 7. Runtime dependencies: bundled vs resolved at runtime

| Dependency | How it is obtained | Evidence |
|---|---|---|
| All npm deps of `packages/*` except HyperFrames | Bundled into `dist/mcp.mjs` by tsdown. `repomix` is aliased to its file-search module | `packages/mcp/tsdown.config.ts` |
| `@hyperframes/producer@0.8.78` | **Never bundled or installed by the plugin.** Resolved from `${CLAUDE_PLUGIN_DATA}/deps/node_modules`, then `<plugin root>/node_modules` ancestors. The user installs it manually (`skills/render/SKILL.md:100-102`) | `packages/mcp/src/hyperframes.ts:67-111` |
| `puppeteer-core` | Resolved at runtime next to the HyperFrames producer, then the plugin root. Used for the Chrome probe and demo capture | `packages/mcp/src/demo.ts:107-134`; `packages/renderer/src/hyperframes-renderer.ts:113` |
| Google Chrome | System install (`CHROME_PATH` or standard locations) | `packages/renderer/src/hyperframes-renderer.ts:78`; `packages/mcp/src/doctor.ts:249-290` |
| `ffmpeg` / `ffprobe` | System binaries (`FFMPEG_PATH`/`FFPROBE_PATH` override, then PATH). Never `ffmpeg-static` | `packages/media/src/ffmpeg.ts:69` |
| `whisper-cli` (whisper.cpp) | System binary. Model `ggml-base.en.bin` downloaded only with `download_model: true`, sha256-pinned | `packages/media/src/asr.ts:49-50`; `packages/mcp/src/transcribe.ts:20-27,95-128` |
| `say` / `espeak-ng` | System TTS by platform | `packages/voice/src/system.ts:150-154` |
| `c2patool` | `C2PATOOL_PATH`, then PATH. Only used for `export sign: true` | `packages/mcp/src/c2pa.ts:120-136` |
| `node:sqlite` | Node 22.13+ built-in, for the ledger | `packages/core/src/ledger.ts:4,213-216` |

## 8. External services: implemented or stub

| Service | Status | Evidence |
|---|---|---|
| ElevenLabs TTS (`/with-timestamps`) | **Implemented** (plain fetch; used by `voice: auto` whenever `ELEVENLABS_API_KEY` is non-empty) | `packages/voice/src/elevenlabs.ts:11,152-177,184-215`; `synthesize.ts:67-71` |
| Hugging Face (whisper model download) | Implemented, consent-gated, sha256-verified | `packages/mcp/src/transcribe.ts:23-25,95-128` |
| Arbitrary http(s) URLs (ingest) | Implemented: 15 s timeout, 5 MB cap, manual redirects (5 max). **No private-address filtering** | `packages/ingestion/src/url.ts:12-15,109-155` |
| Runway, HeyGen, fal.ai, Kling | **Not implemented.** Only the userConfig keys, the `.mcp.json` env mapping, the doctor presence check (`doctor.ts:60-66,312-328`) and a forbidden-terms list (`schema/src/video-spec.ts:513-530`) exist. No adapter, no `VideoProviderAdapter` interface anywhere in `packages/` | grep for `VideoProviderAdapter` finds nothing |
| Generic provider job runner | `JobRunner` exists with ledger persistence and polling, but **nothing outside its own test uses it** | `packages/core/src/jobs.ts:107`; only `jobs.test.ts` imports it |

## 9. Data locations

| Data | Location | Evidence |
|---|---|---|
| Project artifacts | `<project>/source/` (content-ir.json, provenance.json, assets/, transcripts/), `project/` (project.json, creative-brief.yaml, video-spec.json, storyboard.md, variants.json, demo.json, translation.json), `assets/{generated,supplied,voice,music}`, `renders/{preview,final}/` (scenes/, captions/, master.mp4, reel.mp4, render-state.json), `renders/latest.json`, `renders/.render.lock`, `dist/` (+ `dist/<target>/`), `qa/`, `golden/`, and derived sub-projects `shorts/<id>/`, `variants/<id>/`, `localized/<lang>/` | `packages/core/src/project.ts:50-67`; `packages/mcp/src/pipeline.ts:319-321,783-792,966`; `render-lock.ts:6-11` |
| Plugin data dir | `CLAUDE_PLUGIN_DATA`, else `VIDEO_STUDIO_DATA`, else `~/.video-studio`. The code refuses to resolve inside `CLAUDE_PLUGIN_ROOT` | `packages/core/src/data-dir.ts:26-38` |
| Under the data dir | `db/ledger.sqlite` (render jobs), `cache/voice` (CAS + index), `cache/align`, `deps/node_modules` (HyperFrames), `models/ggml-base.en.bin` | `render-jobs.ts:59`; `pipeline.ts:469,501`; `transcribe.ts:79` |
| Temp | `os.tmpdir()/vs-demo-*` for demo capture (removed afterwards) | `packages/mcp/src/demo.ts:269,368` |
